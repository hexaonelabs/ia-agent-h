import { ActiveToolsPipe } from './active-tools.pipe';

describe('ActiveToolsPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveToolsPipe();
    expect(pipe).toBeTruthy();
  });
});
